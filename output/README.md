# Output Artifacts

Generated captures are stored under `output/captures/<capture-id>/`.

Each capture folder contains:

- Raw UI hierarchy dump (`window_dump.xml`)
- Raw screenshot (`screen.png`)
- Canonical dataset (`screen-snapshot.json`)
- Markdown report (`report.md`)
- HTML report (`report.html`)

These files are generated by `scripts/current_screen_report.py`.
