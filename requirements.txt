# Android System Analyzer — RAG-powered system inspector
#
# OpenAI-compatible client used for both chat completions (tool-calling)
# and embeddings (knowledge store retrieval).
openai>=1.40

# Pillow is used to downscale the optional home-screen screenshot before
# sending it to the model so the multimodal context stays compact.
pillow>=10.0

# Test harness (see scripts/run_tests_report.py and tests/).
pytest>=8.0

# Local web UI (rag_run.py serve). Pulls in starlette + uvicorn +
# pydantic. Only required when running the web interface; the CLI
# inspect / ask flows work without it.
fastapi[standard]>=0.110

